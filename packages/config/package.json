{
  "name": "@skilltree/config",
  "description": "Config utilities, API clients, constants",
  "version": "0.1.0",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"No unit tests configured for this package yet\" && exit 0",
    "test:unit": "echo \"No unit tests configured for this package yet\" && exit 0",
    "test:integration": "echo \"No integration tests configured for this package yet\" && exit 0"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
